name: Deploy Supabase Functions

on:
  push:
    branches:
      - claude/stoic-bardeen-DNwHu
      - main
    paths:
      - 'supabase/functions/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy all edge functions
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          for dir in supabase/functions/*/; do
            fn=$(basename "$dir")
            echo "Deploying $fn..."
            supabase functions deploy "$fn" --project-ref vcptpgmsxwynbobsmmdd
            echo "Done: $fn"
          done
